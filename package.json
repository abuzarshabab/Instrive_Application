{
  "name": "instrive",
  "version": "1.0.0",
  "description": "1. Create a new Node.js application. \r 2. Create a new MongoDB database (instrive)\r 3. Create a company info collections in it.\r 4. Create POST API to save data in the collection.\r \ta. Use Joi validation to validate request params.\r \tb. Save uploaded image in base64 format in database\r 5. Create a GET API to retrieve all company information from the company info collection using aggregation framework\r \ta. Add pagination\r \tb. Add sorting\r \tc. Add Search Filtering using the fields(company name, country, product name).\r 6. Set up error handling and logging mechanism\r 7. Add cors, helmet, morgan libraries for security measures\r 8. Add swagger Documentation for both the API's",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "joi": "^17.13.3",
    "mongodb": "^6.8.0",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
